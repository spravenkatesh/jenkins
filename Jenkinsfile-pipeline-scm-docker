pipeline {
    agent dockerContainer
    stages {
        stage('Build') {
            steps {
                echo 'Hello World!!'
                sh 'python3 --version'
            }
        }
    }
}