pipeline {
    agent {
        docker {python:latest-slim}
    }
    stages {
        stage('Build') {
            steps {
                echo 'building python'
            }
        }
        stage('test') {
            steps {
                run 'python3 --version'
            }
        }
    }
}
